name: Temporary Jellyfin gofmt helper

on:
  push:
    branches:
      - restructure/jellyfin-connector

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: restructure/jellyfin-connector
      - uses: actions/setup-go@v5
        with:
          go-version: "1.23.12"
      - run: gofmt -w apps/api/cmd/server apps/api/internal/library
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add apps/api/cmd/server apps/api/internal/library
          git commit -m "style: gofmt Jellyfin connector"
          git push origin HEAD:restructure/jellyfin-connector
