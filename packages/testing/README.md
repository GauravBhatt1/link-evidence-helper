# Contract testing

This package contains deterministic, sanitized fixtures produced from the
current Python aggregation behavior. It performs no live network requests and
does not read production data.

See `python/README.md` for regeneration and validation commands.
